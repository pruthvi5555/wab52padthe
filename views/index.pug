extends layout

block content
  h1= title
  p You always pass failure on the way to success
  button(type="button", class="btn btn-secondary" onclick='update()') press me 
  div(id='target') Display
  a(href='/fans') Search fans results 
  br
  script.
    var client_count = 0;
    function update(){
      client_count++
      document.getElementById("target").innerHTML = `Value now ${client_count}`
    }  